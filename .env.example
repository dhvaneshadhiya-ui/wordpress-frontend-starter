# ============================================================
# iGeeksBlog Frontend - Environment Configuration
# ============================================================
# Copy this file to .env and configure for your environment
# 
# Required: Node.js 18+ (specified in .nvmrc)
# ============================================================

# ------------------------------------------------------------
# CORE CONFIGURATION (Required)
# ------------------------------------------------------------

# WordPress REST API Base URL (backend)
# Points to your WordPress installation's REST API endpoint
VITE_WORDPRESS_API_URL=https://dev.igeeksblog.com/wp-json/wp/v2

# Frontend Site URL (for canonical URLs, sitemaps, and OG images)
# This should be your production frontend domain
SITE_URL=https://wp.dev.igeeksblog.com

# ------------------------------------------------------------
# SEO INDEXING CONTROL
# ------------------------------------------------------------

# Search Engine Indexing
# - Set to 'true' for PRODUCTION (allows Google/Bing indexing)
# - Set to 'false' for STAGING/DEV (blocks all crawlers)
# 
# This controls:
# - Meta robots tag (index/noindex)
# - robots.txt content (allow/disallow all)
# - AI bot access (GPTBot, PerplexityBot, Claude-Web)
VITE_ENABLE_INDEXING=false

# ------------------------------------------------------------
# DEPLOY HOOKS (Optional - for WordPress webhook triggers)
# ------------------------------------------------------------

# Automatic rebuild when WordPress content changes
# Configure ONE of these based on your hosting platform:

# Vercel Deploy Hook:
# Create at: Project Settings > Git > Deploy Hooks
# DEPLOY_HOOK_URL=https://api.vercel.com/v1/integrations/deploy/prj_xxx/yyy

# Netlify Build Hook:
# Create at: Site Settings > Build & deploy > Build hooks
# DEPLOY_HOOK_URL=https://api.netlify.com/build_hooks/xxx

# Webhook Secret (optional security validation)
# Set this in WordPress plugin (wp-plugins/igeeksblog-webhook.php)
# WEBHOOK_SECRET=your-secure-random-string

# ------------------------------------------------------------
# PLATFORM-SPECIFIC FEATURES (Auto-detected)
# ------------------------------------------------------------

# OG Image Generation:
# - Netlify: Uses Edge Function at /og (netlify/edge-functions/og.ts)
# - Vercel: Uses API Route at /api/og (api/og.tsx)
# No configuration needed - detected automatically by src/lib/platform.ts

# Image CDN/Optimization:
# - Netlify: /.netlify/images (configured in netlify.toml)
# - Vercel: /_vercel/image (configured in vercel.json)
# No configuration needed - detected automatically

# ------------------------------------------------------------
# WORDPRESS BACKEND REQUIREMENTS
# ------------------------------------------------------------

# Ensure your WordPress backend has:
# 1. CORS headers allowing your frontend domain
# 2. REST API enabled (default in WP 4.7+)
# 3. Optional: iGeeksBlog plugins for webhooks/previews/redirects
#    See: wp-plugins/ directory for available plugins
#
# Required CORS origins:
# - https://wp.dev.igeeksblog.com (production frontend)
# - https://*.lovableproject.com (Lovable preview)
# - http://localhost:* (local development)
